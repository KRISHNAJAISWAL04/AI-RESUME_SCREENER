services:
  - type: web
    name: resume-screening-agent
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: streamlit run new6.py --server.port $PORT --server.address 0.0.0.0
    envVars:
      - key: PYTHON_VERSION
        value: 3.10.0
